services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: salora
      POSTGRES_PASSWORD: salora
      POSTGRES_DB: salora
    ports: ["5432:5432"]
    volumes: ["salora_data:/var/lib/postgresql/data"]
volumes:
  salora_data:
