"use client"; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {ArrowRight,LockKeyhole} from 'lucide-react'; export default function Login(){const[e,setE]=useState('owner@salora.demo'),[p,setP]=useState('Demo@12345'),[err,setErr]=useState(''),[busy,setBusy]=useState(false),r=useRouter();async function go(x:any){x.preventDefault();setBusy(true);setErr('');try{const q=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},credentials:'include',body:JSON.stringify({email:e,password:p})});if(!q.ok)throw Error((await q.json()).error||'Login failed');r.push('/dashboard')}catch(x:any){setErr(x.message)}finally{setBusy(false)}}return <main className="auth"><form onSubmit={go} className="authcard"><b className="logo"><i/>SALORA</b><small className="eyebrow">STAFF ACCESS</small><h1>Welcome back.</h1><p>Enter the salon command center.</p><label>Email<input value={e} onChange={x=>setE(x.target.value)} type="email"/></label><label>Password<input value={p} onChange={x=>setP(x.target.value)} type="password"/></label>{err&&<div className="err">{err}</div>}<button className="primary wide" disabled={busy}>{busy?'Signing in…':'Sign in'} <ArrowRight size={16}/></button><small className="demo"><LockKeyhole size={14}/> Demo credentials are prefilled</small></form></main>}